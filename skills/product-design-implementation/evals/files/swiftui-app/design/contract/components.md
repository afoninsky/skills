# Checkout summary contract

The accepted summary keeps the existing UIKit coordinator and uses a reusable SwiftUI summary. Required states are default, recoverable price error, disabled confirmation, long localized labels, and all Dynamic Type sizes. Navigation and purchase behavior are fixed.

