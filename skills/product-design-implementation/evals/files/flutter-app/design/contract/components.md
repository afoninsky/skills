# Filter sheet contract

The approved filter sheet uses the existing AppSurfaceTheme and Riverpod state. Required states are populated, empty results, offline/retry, keyboard open, and 200 percent text scale. It must preserve routing and query semantics on Android and iOS.
