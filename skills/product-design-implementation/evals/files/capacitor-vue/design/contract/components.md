# GoalCard contract

The compact card uses existing semantic surface, spacing, radius, and type tokens. It supports default, loading, retryable error, and a three-line title without changing navigation or card actions. The approved slice applies to responsive web and the packaged Android wrapper.

