# Component and state contract

Status: Draft

For each shared component, record its purpose, source, semantic token dependencies, content limits, interaction behavior, accessibility semantics, relevant targets, dependent surfaces, and executable fixtures.

## Required state inventory

Include only applicable states: typical, loading, empty, error, recovery, disabled, selected, focus, long/localized content, large text, reduced motion, permission, offline, and partial data.
