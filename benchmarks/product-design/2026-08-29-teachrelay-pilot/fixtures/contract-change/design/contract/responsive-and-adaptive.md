# Responsive and adaptive behavior

The card reflows in the shared web UI. The Android wrapper adds packaged WebView, safe-area, keyboard, back-navigation, and bridge smoke coverage rather than a duplicate component implementation.
