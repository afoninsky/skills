# Gate recommendation: Pivot

Do not pause indefinitely. The proprietary service is unavailable and procurement is blocked, but that establishes a tool constraint—not that the design intent is unpreservable. Tool names are replaceable implementation choices, and no service account or proprietary format should be the sole durable record.

Use the lowest-fidelity open artifact set that preserves the current decision question:

- Journey, dependency, state, or information structure: editable Mermaid source plus SVG or PDF.
- Rough spatial or interaction work: Excalidraw JSON plus SVG.
- Shared editable interface design: Penpot, where authorized, plus structured and static exports.
- Responsive behavior, keyboard flow, semantics, motion, accessibility, or feasibility: disposable local HTML/CSS/JS plus captures or test output.
- Decision and approval record: stable Markdown or HTML, optionally exported to PDF.

For every migrated artifact, retain its stable ID and version, links to the brief and relevant requirements/evidence/assumptions/direction/gate records, editable source, portable review form, provenance and rights, AI/tool disclosure, fidelity limitations, unsupported claims, and an open fallback or migration route. Preserve the unavailable proprietary artifact as provenance if an authorized export already exists; do not represent a reconstruction as pixel-identical or fully equivalent without evidence.

**Evidence level:** The service and procurement constraints are user-supplied. The assertion that no alternative can preserve the work is **Not yet evidenced**.

**Hard gates:** Provenance and accessibility assurance remain **Not yet evidenced** until the migrated artifacts and any interaction-critical behavior are reviewed. No supplied evidence establishes a hard-gate failure.

**Unknowns:** Blocking Unknowns: exact decision question, existing export availability and rights, required editable behaviors, and named Product Owner. Working Assumptions: an open, multi-artifact reconstruction is permissible and production code will remain untouched.

**Authority:** Proposed outcome is **Pivot**. Approver: **Unassigned — Blocking Unknown**. The next safe action is to inventory the essential information and interaction behavior from authorized sources, select the smallest open artifact set above, create a reversible migration sample, and obtain the named Product Owner’s approval before continuing direction work. If authorized sources cannot support a faithful reconstruction, record that specific loss and escalate it rather than imposing an indefinite pause.
