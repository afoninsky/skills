I can’t run the alternatives in that form. It would invalidate the comparison and create provenance risk.

**Proposed G3 outcome: Iterate.** Human approver: **Unassigned — Blocking Unknown**. Brief version, approved rubric, evidence baseline, constraints, hard gates, and precedent rights are also **Not yet evidenced**.

The requested method conflicts with a defensible direction process:

- Three designers may use the same authorized baseline, but not one shared working file before freeze. Each direction needs an isolated context so sibling layouts, interaction choices, and visual systems do not anchor later work.
- Direction 2 must not “improve Direction 1.” It needs its own structurally distinct charter. Factual corrections can pass through the Steward without exposing Direction 1’s solution.
- A shared dashboard skeleton makes the outputs themed variants, not alternatives. Freeze common requirements, realistic content, hostile states, and constraints—not the solution structure.
- Do not recreate a published example pixel for pixel. Record its source, context, observed date, abstracted principle, rights, transformation, and transfer limits; then create an original expression. Proprietary surfaces, assets, brand language, and interactions are not reusable merely because they are strong precedent.
- Do not average scores. First disqualify directions with unresolved hard-gate failures, then compare survivors against the precommitted rubric while keeping evidence, specialist judgment, owner preference, implementation estimates, and Steward inference separate.
- Do not merge “the best pieces” by collage. If synthesis is explicitly approved, select one coherent structural backbone and import only compatible elements that address documented weaknesses, have evidentiary or specialist support, and preserve the backbone’s mental model. Retest the synthesis against every hard gate and the rubric.

Safe next step: name the Product Owner, approve the brief and frozen comparison contract, give each designer the same baseline plus a distinct charter in separate files, freeze all three, run sameness review and cross-critique, then request a human direction-selection decision. A shared review file can be created only after freeze, containing exports and traceable rationale rather than editable sibling source.
