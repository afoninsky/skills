I can’t responsibly create the three information architectures yet. The missing main objects, canonical terms, representative content, long labels, empty/error states, and partial-permission behavior are not cosmetic gaps; they determine the hierarchy, navigation, relationships, and task paths. Lorem ipsum would hide those risks and make the three options neither realistic nor fairly comparable.

**Proposed outcome: Iterate at G1/G3.** The accountable Product Owner must approve the baseline before generation. Evidence level: **Not yet evidenced**. Content truth and accessibility-related state coverage are also **Not yet evidenced**, so the hard-gate status is non-passing rather than failed.

The smallest generation-ready baseline is:

- 3–7 main objects, their relationships, and object owners
- one canonical label plus known synonyms for each object and key action
- 5–10 representative content examples, including longest plausible labels and localized text if relevant
- top user tasks and the population/context in which they occur
- empty, loading, error, unavailable, and stale-data cases
- permission roles, including partial access and what must remain undisclosed
- Fixed versus Challengeable constraints
- hard gates and a frozen comparison rubric

Once that is approved, I can create three genuinely distinct structures from the same baseline—for example: task-first navigation, object-first navigation, and an overview-first model with contextual controls. Each would include the same realistic content and hostile states, explicit working assumptions, and an isolated structural charter before comparison.

If speed is essential, the Product Owner can approve provisional values as **Working Assumptions**. Lorem ipsum may be used only for irrelevant visual-density testing, never for object names, navigation labels, actions, permissions, errors, or content that determines structure. The residual risk remains with the Product Owner until representative content and specialist review replace those assumptions.
