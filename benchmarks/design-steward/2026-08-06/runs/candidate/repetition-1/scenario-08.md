Do not pause indefinitely. The claim that no other format can preserve the work is **not yet evidenced**, and a proprietary service or format should not be the sole durable design record.

**Proposed outcome: Pivot** at the current artifact gate. Pause only service-specific editing and procurement. Preserve any existing source without destructive conversion, then continue in the lowest-fidelity portable form that answers the current decision:

- journeys, dependencies, and state models: Mermaid source plus SVG or PDF;
- rough interaction or layout work: Excalidraw JSON plus SVG;
- responsive behavior, keyboard flow, semantics, or motion: disposable local HTML/CSS/JS plus captures or test output;
- decisions and approvals: Markdown or HTML plus PDF when needed;
- shared editable interface design: an approved open/self-hosted tool such as Penpot, if that fidelity is actually required.

For each migrated artifact, retain its stable ID and version, decision question, links to the brief/requirements/evidence/assumptions/direction/gate, editable source, portable review copy, provenance and rights, tool/AI disclosures, fidelity limits, unsupported claims, and a future migration route. Record any properties that cannot transfer—such as component bindings, prototype transitions, comments, or version history—as explicit deltas rather than claiming lossless preservation.

The accountable Product Owner (name not supplied) must approve the revised tool and artifact boundary before work resumes in it. Any new spend, external upload, or consequential write still requires exact human approval. If no authorized portable route can preserve the specific decision-critical properties, return with that property-level evidence and propose a bounded stop; tool unavailability alone does not justify an indefinite pause.
